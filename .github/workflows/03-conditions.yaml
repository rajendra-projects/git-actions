name: conditions-practise
on: [push]
jobs:
  raj-condition:
    runs-on: ubuntu-latest
    steps:
        if: github.ref == 'refs/heads/feature-**'
      - name: step1
        run: echo "step 1 running"
      - name: step2
        run: echo "step 2 running"
        if: github.ref == 'refs/heads/main'
      - name: step1
        run: echo "step 1 running"
      - name: step3
        run: echo "step 3 running"